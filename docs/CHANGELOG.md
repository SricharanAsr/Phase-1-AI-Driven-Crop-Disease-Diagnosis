# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Comprehensive `.gitignore` for Python and Machine Learning environments.
- Source code directory structure (`src/classification`, `src/detection`, `src/segmentation`, `src/utils`).
- Package initialization `__init__.py` files.
- `data/README.md` for dataset documentation.
- Colorized logging utility in `src/utils/logger.py`.
- Swin Transformer model template in `src/classification/model.py`.
- YAML-based configuration loader in `src/utils/config.py`.
- Detailed setup and installation instructions in `README.md`.

### Changed
- Refined `requirements.txt` with pinned versions and added utility dependencies.
